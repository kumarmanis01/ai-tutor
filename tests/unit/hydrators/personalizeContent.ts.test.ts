/**
 * FILE OBJECTIVE:
 * - Compile/import test for hydrators/personalizeContent.ts
 *
 * LINKED UNIT TEST:
 * - tests/unit/hydrators/personalizeContent.ts.test.ts
 *
 * EDIT LOG:
 * - 2026-01-02T17:08:41.411Z | copilot | autogenerated conversion
 */

import path from 'path'

describe('hydrators/personalizeContent.ts', () => {
  test('module imports and basic exports', () => {
    const p = path.join(process.cwd(), 'hydrators/personalizeContent.ts')
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const mod = require(p)
    if (!mod) throw new Error('module not defined')
  })
})
