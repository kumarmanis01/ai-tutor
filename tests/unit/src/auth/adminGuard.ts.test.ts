/**
 * FILE OBJECTIVE:
 * - Compile/import test for src/auth/adminGuard.ts
 *
 * LINKED UNIT TEST:
 * - tests/unit/src/auth/adminGuard.ts.test.ts
 *
 * EDIT LOG:
 * - 2026-01-02T17:08:41.640Z | copilot | autogenerated conversion
 */

import path from 'path'

describe('src/auth/adminGuard.ts', () => {
  test('module imports and basic exports', () => {
    const p = path.join(process.cwd(), 'src/auth/adminGuard.ts')
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const mod = require(p)
    if (!mod) throw new Error('module not defined')
  })
})
