/**
 * FILE OBJECTIVE:
 * - Compile/import test for scripts/mark-admin.ts
 *
 * LINKED UNIT TEST:
 * - tests/unit/scripts/mark-admin.ts.test.ts
 *
 * EDIT LOG:
 * - 2026-01-02T17:08:41.620Z | copilot | autogenerated conversion
 */

import path from 'path'

describe('scripts/mark-admin.ts', () => {
  test('module imports and basic exports', () => {
    const p = path.join(process.cwd(), 'scripts/mark-admin.ts')
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const mod = require(p)
    if (!mod) throw new Error('module not defined')
  })
})
