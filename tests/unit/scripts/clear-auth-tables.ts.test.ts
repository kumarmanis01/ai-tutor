/**
 * FILE OBJECTIVE:
 * - Compile/import test for scripts/clear-auth-tables.ts
 *
 * LINKED UNIT TEST:
 * - tests/unit/scripts/clear-auth-tables.ts.test.ts
 *
 * EDIT LOG:
 * - 2026-01-02T17:08:41.615Z | copilot | autogenerated conversion
 */

import path from 'path'

describe('scripts/clear-auth-tables.ts', () => {
  test('module imports and basic exports', () => {
    const p = path.join(process.cwd(), 'scripts/clear-auth-tables.ts')
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const mod = require(p)
    if (!mod) throw new Error('module not defined')
  })
})
