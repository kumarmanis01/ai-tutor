/**
 * FILE OBJECTIVE:
 * - Wrapper to import the UI spec so the project's test glob discovers and runs it.
 *
 * LINKED UNIT TEST:
 * - tests/unit/components/parent/subscription/ParentUpgradeFlow.spec.tsx
 *
 * EDIT LOG:
 * - 2026-04-13T05:30:00Z | copilot | add wrapper test to include UI spec in test run
 */

import './ParentUpgradeFlow.spec.tsx'
