/**
 * FILE OBJECTIVE:
 * - Compile/import test for .vscode/types/global.d.ts
 *
 * LINKED UNIT TEST:
 * - tests/unit/.vscode/types/global.d.ts.test.ts
 *
 * EDIT LOG:
 * - 2026-01-02T17:08:41.035Z | copilot | autogenerated conversion
 */

import path from 'path'

describe('.vscode/types/global.d.ts', () => {
  test('module imports and basic exports', () => {
    const p = path.join(process.cwd(), '.vscode/types/global.d.ts')
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const mod = require(p)
    if (!mod) throw new Error('module not defined')
  })
})
