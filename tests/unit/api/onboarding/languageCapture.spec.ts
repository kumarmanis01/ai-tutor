/**
 * FILE OBJECTIVE:
 * - Tests for onboarding language preference capture endpoint (placeholder).
 *
 * LINKED UNIT TEST:
 * - tests/unit/api/onboarding/languageCapture.spec.ts
 *
 * COPILOT INSTRUCTIONS FOLLOWED:
 * - /docs/COPILOT_GUARDRAILS.md
 * - .github/copilot-instructions.md
 *
 * EDIT LOG:
 * - 2026-04-13T00:00:00Z | copilot | add onboarding language capture test skeleton
 */

// TODO: wire to real API route (e.g. app/api/onboarding/language/route.ts) and DB mocks
describe('Onboarding language capture (API)', () => {
  it('persists selected language for a new user (placeholder)', () => {
    // TODO: mock session + prisma.user.update and assert persisted language
    expect(true).toBe(true);
  });
});
