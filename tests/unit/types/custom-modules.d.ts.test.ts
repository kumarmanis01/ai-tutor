/**
 * FILE OBJECTIVE:
 * - Compile/import test for types/custom-modules.d.ts
 *
 * LINKED UNIT TEST:
 * - tests/unit/types/custom-modules.d.ts.test.ts
 *
 * EDIT LOG:
 * - 2026-01-02T17:08:41.682Z | copilot | autogenerated conversion
 */

import path from 'path'

describe('types/custom-modules.d.ts', () => {
  test('module imports and basic exports', () => {
    const p = path.join(process.cwd(), 'types/custom-modules.d.ts')
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const mod = require(p)
    if (!mod) throw new Error('module not defined')
  })
})
