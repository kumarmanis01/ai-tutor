/**
 * FILE OBJECTIVE:
 * - Compile/import test for types/accept-language-parser.d.ts
 *
 * LINKED UNIT TEST:
 * - tests/unit/types/accept-language-parser.d.ts.test.ts
 *
 * EDIT LOG:
 * - 2026-01-02T17:08:41.680Z | copilot | autogenerated conversion
 */

import path from 'path'

describe('types/accept-language-parser.d.ts', () => {
  test('module imports and basic exports', () => {
    const p = path.join(process.cwd(), 'types/accept-language-parser.d.ts')
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const mod = require(p)
    if (!mod) throw new Error('module not defined')
  })
})
