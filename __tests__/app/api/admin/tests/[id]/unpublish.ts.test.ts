/* AUTO-GENERATED TEST STUB */
import path from 'path'

describe('app/api/admin/tests/[id]/unpublish.ts', () => {
  test('exports loadable', () => {
    const p = path.join(process.cwd(), 'app/api/admin/tests/[id]/unpublish.ts');
    // Import safely using require to avoid ESM resolution issues in tests
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const mod = require(p);
    expect(mod).toBeDefined();
  });
});
