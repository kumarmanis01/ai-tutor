Migration: drop lastFreeQuestionsUpdate

This migration removes the `lastFreeQuestionsUpdate` column from the `User` table.

Notes:

- The column is dropped only if it exists.
- This migration SQL is non-reversible by default; ensure you have a backup if you need to restore data.
- To apply to production, run:
  `DATABASE_URL="<your_prod_db_url>" npx prisma migrate deploy`

Generated by automation on 2025-11-27.
