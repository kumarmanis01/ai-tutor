Initial baseline migration generated by diff from empty -> schema.prisma on 2025-11-27.

This migration represents the current schema as a single initial migration. It was created by running:

npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script > prisma/migrations/20251127_initial/migration.sql

Notes:

- The database may already contain objects (enums/types) if you previously applied migrations; this migration is intended as a baseline for future migrations.
- To apply this migration in CI, use `npx prisma migrate deploy`.
