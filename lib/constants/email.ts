/**
 * Email address constants used across API routes and background jobs.
 * Centralised here so addresses are never hardcoded in business logic.
 */

/** Receives content-issue reports from students. */
export const EMAIL_CONTENT_REPORT = 'content@spinzyacademy.com';

/** General support inbox. */
export const EMAIL_SUPPORT = 'support@spinzyacademy.com';

/** Ops/admin notifications (cost reports, alerts). */
export const EMAIL_OPS = 'ops@spinzyacademy.com';
